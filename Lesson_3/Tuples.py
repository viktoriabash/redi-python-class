# NOT CHANGABLE; IMMUTABLE, 
# '' allocate smth
#with round brackets  ()
person = ("Female", 37, "Anna")
#with tuple() function
# person = tuple("abc")
print(person)
print(person[0]) # = 'b'
#-------------
list_coordinates = [1,2,3,4,5,6,7,8] # we want to allocate tuples fromt he list
# listoftuples = []

# for i in 
first_tuple = tuple(list_coordinates[0:2])

print(first_tuple)


print(len(first_tuple))
persona = ('Anna', 37)

# i go through the list of coordinates and every pair in the list is added to a tuple and append the list of tuples
list_coordinates = [1,2,3,4,5,6,7,8] 

first_tuple = tuple(list_coordinates[0:2])
second_tuple = tuple()
